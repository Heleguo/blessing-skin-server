<!DOCTYPE html>
<html lang="{{ locale }}">
  <head>
    {{ include('shared.head') }}
    <title>{% block title %}{% endblock %} - {{ site_name }}</title>
  </head>

  <body class="{{ dark_mode ? 'dark-mode' : '' }} hold-transition layout-top-nav">
    <div class="wrapper">
      <nav class="main-header navbar navbar-expand navbar-white navbar-light ml-0" style="border-bottom: 1px solid var(--heleguo-border-color);">
        <div class="container">
          <div class="navbar-header">
            <a href="{{ url('/') }}" class="navbar-brand" style="font-family: 'Inter Tight', sans-serif; font-weight: 800; color: var(--heleguo-orange);">{{ site_name }}</a>
          </div>

          <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
              <li class="nav-item active">
                <a class="nav-link" href="{{ url('skinlib') }}" style="color: var(--heleguo-text-secondary);">
                  {{ trans('general.skinlib') }}
                </a>
              </li>
              {% if auth_check() %}
                <li class="nav-item">
                  <a class="nav-link" href="{{ url('user/closet') }}" style="color: var(--heleguo-text-secondary);">
                    {{ trans('general.my-closet') }}
                  </a>
                </li>
              {% endif %}
            </ul>
          </div>

          <div class="navbar-custom-menu">
            <ul class="nav navbar-nav">
              {% if auth_check() %}
                <li class="nav-item">
                  <a class="nav-link" href="{{ url('skinlib/upload') }}" style="color: var(--heleguo-text-secondary);">
                    <i class="fas fa-upload" aria-hidden="true"></i>&nbsp;
                    <span class="d-none d-sm-inline">
                      {{ trans('skinlib.general.upload-new-skin') }}
                    </span>
                  </a>
                </li>
                {{ include('shared.notifications') }}
              {% endif %}
              {{ include('shared.languages') }}
              {% if auth_check() %}
                {{ include('shared.user-menu') }}
              {% else %}
                <li class="nav-item">
                  <a class="nav-link" href="{{ url('auth/login') }}" style="color: var(--heleguo-text-secondary);">
                    <i class="fas fa-user"></i>
                    <span class="d-none d-sm-inline">
                      {{ trans('general.anonymous') }}
                    </span>
                  </a>
                </li>
              {% endif %}
            </ul>
          </div>
        </div>
      </nav>

      {% block content %}{% endblock %}

      <footer class="main-footer">
        <div class="container">
          {{ include('shared.copyright') }}
        </div>
      </footer>
    </div>
    {% block before_foot %}{% endblock %}
    {{ include('shared.foot') }}
  </body>
</html>
